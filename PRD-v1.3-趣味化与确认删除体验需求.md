# 拾遗 PickUp v1.3 趣味化与确认删除体验需求文档

> **文档类型**: 产品需求文档（PRD）  
> **版本**: v1.3  
> **日期**: 2026-05-31  
> **状态**: 已完成
> **关联版本**: v1.2 / v1.10 已完成能力  
> **目标平台**: Android / iOS  

---

## 1. 版本目标

v1.3 的目标是在不改变核心照片整理流程的前提下，提升关键决策点的质感，并为产品加入轻量趣味化能力，让用户在完成清理后获得更明确的反馈、更强的成就感和持续使用动机。

本版本聚焦 4 个方向：

1. 重设计确认删除对话框，使删除前确认更符合当前暗色、金色强调的整体风格。
2. 强化个人中心统计卡片，让连续天数、最近删除、释放空间等核心数据更醒目。
3. 在个人中心新增“每周清理回顾”功能。
4. 在个人中心新增“成就系统”功能。

---

## 2. 设计参考

### 2.1 iOS / Apple HIG 参考原则

参考 Apple Human Interface Guidelines 中关于 Alerts 的建议：

- 删除确认属于破坏性操作，应只在确有必要时打断用户。
- 文案应短、明确、可行动。
- 操作按钮需要清晰表达后果，避免只写“确定”这类模糊文案。
- 删除类操作应保留取消路径，并降低误触风险。

参考链接：

- Apple HIG - Alerts: https://developer.apple.com/design/human-interface-guidelines/alerts/
- Apple UIKit Alerts and Action Sheets: https://developer.apple.com/documentation/uikit/windows_and_screens/getting_the_user_s_attention_with_alerts_and_action_sheets

### 2.2 视觉灵感参考

参考 Dribbble 上 confirmation modal、achievement mobile UI 的常见趋势：

- 半透明深色遮罩 + 居中卡片或底部 sheet。
- 大数字/大图标作为视觉锚点。
- 危险操作使用红色，但不大面积铺满，避免廉价感。
- 成就系统常使用徽章、进度环、解锁态/未解锁态对比。

参考链接：

- Dribbble Confirm Action Modal: https://dribbble.com/search/confirm-action-modal
- Dribbble Achievements UI: https://dribbble.com/tags/achievements-ui

---

## 3. 当前项目基础

### 3.1 当前已具备能力

- `DeleteConfirmSheet` 已经是自定义 React Native `Modal`，不是原生 `Alert.alert`。
- 删除服务 `delete-service.ts` 已支持删除前缓存缩略图，并写入 `recentDeletes`。
- 个人中心 `settings.tsx` 已展示 4 个统计卡片：
  - 已浏览
  - 最近删除
  - 连续天数
  - 释放空间
- 最近删除数量当前使用 `getValidRecentDeletes()` 动态读取有效最近删除记录。
- 当前整体视觉以黑色背景、黄色强调色、圆角卡片为主。

### 3.2 设计约束

- 不引入重型新依赖，优先使用当前已有能力：
  - React Native `Modal`
  - `react-native-reanimated`
  - `expo-blur` / 项目内 `SafeBlurView`
  - `@expo/vector-icons`
  - AsyncStorage
- 不做云端同步，不上传照片。
- 趣味化功能只依赖本地数据，优先从已有统计数据派生。
- iOS 和 Android 均需可用。

---

## 4. 需求列表

| 编号 | 需求名称 | 优先级 | 类型 |
|---|---|---:|---|
| REQ-01 | 确认删除对话框重设计 | P0 | UI / 交互 |
| REQ-02 | 个人中心统计卡片醒目色彩强化 | P1 | UI |
| REQ-03 | 每周清理回顾 | P1 | 功能 / 趣味化 |
| REQ-04 | 成就系统 | P1 | 功能 / 趣味化 |

---

## REQ-01: 确认删除对话框重设计

### 背景

当前 `DeleteConfirmSheet` 已经是自定义弹窗，但视觉仍偏基础：主要由标题、说明文案、删除按钮、取消按钮组成。用户希望参考 iOS design 和优秀移动端 UI 案例，将确认删除界面做得更精致，并符合 PickUp 当前暗色 + 金色强调的风格。

### 目标

将确认删除对话框升级为更有质感、更清晰、更符合破坏性操作心智的自定义弹窗。

### 设计方向

推荐方案：**iOS 风格玻璃拟态危险确认卡片**。

核心视觉：

- 深色遮罩，背景轻微压暗。
- 中央浮层卡片，圆角 28 左右。
- 顶部使用危险操作图标，例如 `delete-outline` 或 `alert-circle-outline`。
- 主视觉突出删除数量，例如：
  - `删除 12 张照片？`
  - 数字 `12` 使用更大字号。
- 文案说明保持短句：
  - `这些照片会进入系统最近删除，你仍可在限定时间内恢复。`
- 主按钮使用红色危险色。
- 取消按钮使用低强调透明按钮。
- 可选展示一条轻量提示：
  - `删除前已为最近删除记录生成本地缩略图。`

### 交互规则

| 场景 | 行为 |
|---|---|
| 点击删除按钮 | 执行 `onConfirm` |
| 删除中 | 删除按钮进入 loading 态，禁用重复点击 |
| 点击取消 | 执行 `onCancel` 并关闭 |
| 点击遮罩 | 关闭弹窗 |
| Android 返回键 | 关闭弹窗 |
| count = 1 | 文案使用单张语义 |
| count > 1 | 文案使用批量语义 |

### 动效要求

- 弹窗出现：遮罩 180-240ms fade in，卡片 scale 0.96 -> 1，opacity 0 -> 1。
- 弹窗关闭：遮罩 fade out，卡片轻微 scale down。
- 删除按钮点击时可加入轻微 press scale，不超过 0.97。
- 动效要克制，不做夸张弹跳。

### 技术范围

主要修改：

- `src/components/delete-review/DeleteConfirmSheet.tsx`

可选修改：

- `src/design-tokens.ts`
- `src/components/ui/SafeBlurView.tsx`

### 验收标准

- [ ] 删除确认弹窗不使用原生 `Alert.alert`。
- [ ] 弹窗视觉与当前黑色背景、黄色强调风格一致。
- [ ] 主删除动作是明确的危险按钮，取消动作清晰可见。
- [ ] 删除中不能重复点击确认。
- [ ] Android 返回键可关闭弹窗。
- [ ] iOS / Android 布局不溢出、不遮挡、不出现文字截断。
- [ ] `npx.cmd tsc --noEmit` 通过。
- [ ] `npx.cmd jest --runInBand` 通过。

---

## REQ-02: 个人中心统计卡片醒目色彩强化

### 背景

当前个人中心统计卡片视觉偏统一，核心数据不够醒目。用户希望连续天数、最近删除等区域的字体颜色变成黄色、绿色等更醒目的颜色，让统计区更有活力。

### 目标

在保持整体暗色 UI 统一的前提下，为不同统计指标分配语义化强调色，提高信息识别度。

### 色彩建议

| 卡片 | 建议强调色 | 语义 |
---|---|---|
| 已浏览 | `#FFFFFF` 或浅灰 | 基础浏览数据，低情绪 |
| 最近删除 | `#FF453A` 红色 | 删除/清理动作 |
| 连续天数 | `#34C759` 绿色 | 连续、成长、正反馈 |
| 释放空间 | `#FFCC00` 黄色 | 空间收益、成就感 |

### UI 规则

- 只强化数值，不建议整张卡片大面积变色。
- label 保持较低强调，避免统计区过于嘈杂。
- 数字颜色可通过 `StatCard` 新增 `valueColor` 参数实现。
- 可为卡片添加非常轻的同色描边或背景 tint，但透明度需低。
- 不使用彩虹式混乱配色，保持 3-4 个明确语义色。

### 技术范围

主要修改：

- `src/components/settings/StatCard.tsx`
- `app/settings.tsx`

### 验收标准

- [ ] 最近删除数值为红色系。
- [ ] 连续天数数值为绿色系。
- [ ] 释放空间数值为黄色系。
- [ ] 已浏览保持白色或浅灰，避免喧宾夺主。
- [ ] 颜色在暗色背景下具备足够对比度。
- [ ] 不影响 StatCard 原有布局。

---

## REQ-03: 每周清理回顾

### 背景

用户完成照片整理后，目前只能看到静态统计数据。缺少周期性反馈，无法形成“这一周我整理了多少”的成就感。每周清理回顾可以把已有数据转化为更有趣、更可感知的总结。

### 目标

在个人中心中部新增“每周清理回顾”模块，让用户看到最近 7 天的清理成果。

### 功能入口

位置：个人中心中部，建议位于“统计”区下方、“帮助”区上方。

模块标题：

- `每周清理回顾`

展示形态：

- 一张横向回顾卡片。
- 不需要进入二级页面，v1.3 先做单卡片展示。

### 展示内容

每周回顾卡片展示：

| 字段 | 数据来源 | 示例 |
|---|---|---|
| 本周浏览 | `weeklyHistory.viewed` 求和 | `本周浏览 126 张` |
| 本周删除 | `weeklyHistory.deleted` 求和 | `清理 18 张` |
| 连续天数 | `streakDays` | `连续整理 4 天` |
| 本周结论文案 | 派生规则 | `这周你给相册松了一口气` |

可选视觉：

- 一条 7 天 mini bar。
- 每天一个小柱，显示当天删除数量。
- 当前没有数据时显示轻空态：
  - `本周还没有清理记录`
  - `整理一组照片后，这里会生成你的周回顾`

### 文案规则

| 条件 | 文案 |
|---|---|
| deleted = 0 | `本周还没有清理记录` |
| deleted 1-9 | `这周完成了一次轻整理` |
| deleted 10-49 | `这周相册清爽了不少` |
| deleted >= 50 | `这周你完成了一次大扫除` |

### 数据规则

- 使用已有 `weeklyHistory`，不新增云端数据。
- 按最近 7 条或最近 7 天数据展示。
- 如果当天无记录，对应 bar 显示为 0。
- 该模块只读，不修改统计数据。

### 技术范围

可能新增：

- `src/components/settings/WeeklyReviewCard.tsx`

可能修改：

- `app/settings.tsx`
- `src/contexts/StatsContext.tsx`（如当前未暴露 `weeklyHistory`，需要补充暴露）
- `src/services/stats-service.ts`

### 验收标准

- [ ] 个人中心展示“每周清理回顾”模块。
- [ ] 有数据时展示本周浏览、本周删除、连续天数和总结文案。
- [ ] 无数据时展示空态。
- [ ] 7 天 mini bar 数据与 `weeklyHistory` 一致。
- [ ] 不影响现有统计卡片。
- [ ] App 重启后数据仍正确。

---

## REQ-04: 成就系统

### 背景

照片清理本质上是低频、带负担的工具型行为。成就系统可以把整理过程转化为轻量游戏化反馈，提高完成感和留存。

### 目标

在个人中心中部新增“成就系统”模块，展示用户已解锁/未解锁的整理成就。

### 功能入口

位置：个人中心中部，建议与“每周清理回顾”相邻。

模块标题：

- `成就`

展示形态：

- v1.3 先做横向滚动徽章列表。
- 每个徽章包含图标、名称、状态、进度。
- 不做复杂二级页，减少范围。

### 首批成就设计

| 成就 | 解锁条件 | 图标建议 | 色彩 |
|---|---:|---|---|
| 初次整理 | `totalViewed >= 1` | `sparkles` | 黄色 |
| 清理新手 | `recentDeleteCount >= 10` 或 `totalDeleted >= 10` | `delete-sweep-outline` | 红色 |
| 空间救星 | `totalFreedBytes >= 500MB` | `database-arrow-down-outline` | 黄色 |
| 连续行动 | `streakDays >= 3` | `fire` / `calendar-check` | 绿色 |
| 相册管家 | `totalViewed >= 500` | `image-multiple-outline` | 蓝色 |

### 进度规则

- 已解锁：高亮显示，展示完成态。
- 未解锁：低透明度显示，展示 `当前 / 目标`。
- 进度不可超过 100%。
- 成就数据本地派生，不需要单独持久化。
- 如后续需要“首次解锁弹窗”，另开需求，不放入 v1.3。

### 推荐展示文案

| 状态 | 示例 |
|---|---|
| 已解锁 | `已解锁` |
| 未解锁 | `7 / 10` |
| 空态 | `开始整理照片后会点亮成就` |

### 技术范围

可能新增：

- `src/components/settings/AchievementStrip.tsx`
- `src/utils/achievement-utils.ts`

可能修改：

- `app/settings.tsx`
- `src/contexts/StatsContext.tsx`

### 验收标准

- [ ] 个人中心展示“成就”模块。
- [ ] 至少包含 5 个首批成就。
- [ ] 成就状态根据本地统计数据实时计算。
- [ ] 未解锁成就展示进度。
- [ ] 已解锁成就视觉高亮。
- [ ] 不引入云端依赖。
- [ ] App 重启后成就状态保持一致。

---

## 5. 非目标范围

以下内容不纳入 v1.3：

- 社交分享海报。
- 云端账号同步。
- AI 自动识别照片好坏。
- 复杂成就二级页面。
- 最近删除恢复功能。
- App Store / Google Play 上架材料制作。
- Expo SDK 升级。

---

## 6. 数据与隐私

- v1.3 不上传照片。
- v1.3 不上传统计数据。
- 每周回顾和成就系统只使用本地已有统计。
- 不读取照片内容做 AI 分析。
- 删除确认弹窗不得暗示照片会被上传或云端处理。

---

## 7. 兼容性要求

- Android release 包必须正常构建。
- iOS 设计需遵守安全区和动态文字基本约束。
- 弹窗、卡片和横向滚动模块在小屏设备上不得溢出。
- 颜色不应只靠色相表达状态，成就状态还需配合文字或图标。

---

## 8. 验收总清单

- [x] 确认删除弹窗完成视觉重设计。
- [x] 删除确认交互不回退到原生 Alert。
- [x] 个人中心统计卡片数值具备语义强调色。
- [x] 每周清理回顾模块完成。
- [x] 成就模块完成。
- [x] 所有新增文案为中文，且无乱码。
- [x] `npx.cmd tsc --noEmit` 通过。
- [x] `npx.cmd jest --runInBand` 通过。
- [x] Android release 本地打包通过。

---

## 9. 推荐实施顺序

1. `REQ-01` 确认删除弹窗重设计。
2. `REQ-02` 统计卡片色彩强化。
3. `REQ-03` 每周清理回顾。
4. `REQ-04` 成就系统。
5. 统一 UI 回归和 Android release 打包验证。

---

## 10. 风险与注意事项

- `StatsContext` 当前如果没有暴露 `weeklyHistory`，需要补充上下文数据出口。
- 成就系统不要误用“最近删除数量”作为历史累计指标；如果需要累计删除，用 `totalDeleted`。
- 最近删除数量当前是有效本地最近删除记录数，不等同于总删除数。
- 删除确认弹窗可以自定义，但系统权限弹窗和系统级删除确认无法自定义。
- `SafeBlurView` 在 Android 上需保留 fallback，避免 blur 原生层级问题影响内容显示。

---

# v1.3 补充需求 — 删除确认布局、空间统计、Review 缩放与功能归位

> **版本补充**: v1.3.1
> **日期**: 2026-06-01
> **状态**: 已完成
> **来源**: 真机体验反馈
> **目标**: 修复当前删除确认与 Review 场景中的可用性问题，并重构“更多功能”的信息架构，让底部中间 Tab 成为真正的功能聚合入口。

## 11. 补充版本目标

本轮补充需求聚焦 4 个体验断点：

1. 待删除确认页的视觉重心偏低，需要整体上移，让标题、说明和照片堆叠区域更自然。
2. 删除按钮后展示的文件大小长期为 `0B`，削弱用户对清理效果的信任，需要修复空间估算与展示链路。
3. Review 照片预览中，双指捏合与双指点击仍无法可靠放大/缩小，需要补齐真实可用的缩放交互。
4. 个人中心承载了太多“更多功能”内容，需要迁移到底部中间 Tab 对应的“更多功能”页面，并重新规划页面布局。

---

## 12. 补充需求清单

| 编号 | 需求名称 | 优先级 | 类型 |
|---|---|---:|---|
| REQ-05 | 待删除确认页布局上移与视觉重心调整 | P1 | UI / 交互 |
| REQ-06 | Delete 按钮文件大小 0B 修复 | P0 | Bug / 数据 |
| REQ-07 | Review 照片双指捏合与双指点击缩放 | P0 | 交互 / 安全确认 |
| REQ-08 | 更多功能迁移至中间 Tab 并重构 Hub 布局 | P1 | 信息架构 / UI |

---

## REQ-05: 待删除确认页布局上移与视觉重心调整

### 背景

当前待删除确认页的左上角返回按钮位置偏低，页面中部“准备收工了吗”文案与下方照片堆叠区域也偏下。整体视觉重心落在屏幕下半部分，导致页面看起来不够紧凑，尤其在小屏或高屏设备上，顶部留白与主体内容关系不自然。

### 目标

优化待删除确认页的垂直布局，使返回按钮、标题区和照片堆叠区整体上移，形成更稳定、更精致的确认页构图。

### 设计规格

| 区域 | 当前问题 | 目标调整 |
|---|---|---|
| 左上角返回按钮 | 距离顶部偏大，视觉上像落在内容区内 | 上移约 `8-16px`，贴近安全区下方 |
| 主标题区 | “准备收工了吗”偏低，页面顶部留白过多 | 标题与副标题整体上移约 `12-24px` |
| 照片堆叠区 | 与标题距离偏松，且整体偏下 | 随标题一起上移，保持标题与图片之间的自然间距 |
| 底部操作区 | 不应被上移影响 | 保持 Delete / 稍后删除按钮安全区适配 |

### 交互规则

- 返回按钮点击行为保持不变。
- “Delete”主按钮、“稍后删除”次按钮行为保持不变。
- 页面内容上移不能导致小屏设备上照片堆叠与按钮重叠。
- 如屏幕高度不足，应优先压缩照片堆叠区域高度，而不是遮挡底部按钮。

### 技术范围

主要修改：

- `src/components/delete-review/DeleteConfirmSheet.tsx`

可能涉及：

- `app/review.tsx`
- `src/utils/delete-confirm-utils.ts`

### 验收标准

- [ ] 左上角返回按钮比当前版本更靠近顶部安全区，视觉不再偏低。
- [ ] “准备收工了吗”标题、副标题和照片堆叠区整体上移，页面重心更自然。
- [ ] 小屏设备上标题、照片和按钮不重叠。
- [ ] 大屏设备上顶部不出现过度空白。
- [ ] 删除确认页所有原有操作行为保持不变。

---

## REQ-06: Delete 按钮文件大小 0B 修复

### 背景

待删除确认页的 `Delete` 按钮后方会展示本次预计释放空间，但当前一直显示 `0B`。这通常说明待删除照片的 `fileSize` 没有正确传入确认页，或删除前文件大小获取逻辑对 `content://` / `ph://` 等媒体 URI 不兼容，导致累计值始终为 0。

### 目标

修复本次待删除照片空间统计链路，让 `Delete` 按钮展示可信的预计释放空间，例如：

- `Delete · 8.4 MB`
- `Delete · 126 MB`

当无法获取真实文件大小时，应使用稳定估算值兜底，不允许在有照片待删除时展示 `0B`。

### 数据规则

| 场景 | 目标行为 |
|---|---|
| 照片已有 `fileSize > 0` | 直接使用 `fileSize` |
| `fileSize = 0` 或缺失 | 使用文件系统 API 尝试获取真实大小 |
| 文件系统 API 失败 | 使用像素估算法兜底 |
| 多张照片 | 对每张照片大小求和 |
| 视频 / Live Photo | 若无法准确读取，优先使用可取得的媒体文件大小；否则用像素估算并避免显示 0 |

### 估算策略

当真实文件大小不可用时，使用当前项目已采用的像素估算法：

```ts
Math.round(width * height * 3 * 0.4)
```

该值作为 JPEG 压缩后的近似估算，不追求绝对精确，但必须避免用户看到 `0B`。

### 技术范围

主要修改：

- `src/services/delete-service.ts`
- `src/utils/delete-confirm-utils.ts`
- `src/components/delete-review/DeleteConfirmSheet.tsx`

可能涉及：

- `src/types/photo.ts`
- `app/review.tsx`
- `app/index.tsx`

### 验收标准

- [ ] 有待删除照片时，`Delete` 按钮后方不再显示 `0B`。
- [ ] 多张照片时展示总大小，而不是单张大小。
- [ ] 文件大小格式化正确：`B / KB / MB / GB`。
- [ ] Android 媒体 URI 无法直接读取时，仍能通过估算值展示非 0 数字。
- [ ] 删除成功后，个人中心“释放空间”累计值与本次删除结果保持同一套计算逻辑。
- [ ] 单元测试覆盖 `fileSize = 0` 时的兜底估算。

---

## REQ-07: Review 照片双指捏合与双指点击缩放

### 背景

Review 页面用于最终确认待删除照片，是避免误删的关键场景。当前虽然已有照片预览能力，但双指捏合和双指点击照片仍无法可靠放大，用户不能快速查看照片细节。

### 目标

在 Review 照片预览中实现可靠、自然的缩放体验：

1. 双指捏合可以连续放大 / 缩小。
2. 双指点击可以在放大和还原之间切换。
3. 放大后可以拖动查看局部细节。

### 交互规格

| 手势 | 行为 |
|---|---|
| 双指捏合向外 | 照片放大，最大 `5x` |
| 双指捏合向内 | 照片缩小，最小回弹到 `1x` |
| 双指点击照片 | 若当前为 `1x`，放大到 `2x`；若当前大于 `1x`，还原到 `1x` |
| 放大后单指拖动 | 平移照片，查看局部 |
| 关闭预览后再次打开 | 缩放和平移状态重置 |
| 单击关闭区域 / 关闭按钮 | 关闭预览，回到 Review 网格 |

### 设计边界

- 缩放范围建议：`1x ~ 5x`。
- 双指点击是辅助快捷操作，不能影响单击关闭或长按预览逻辑。
- 手势冲突优先级：缩放预览内的手势优先于页面滚动。
- 关闭预览后，Review 网格中已勾选 / 未勾选状态不能丢失。

### 技术方案

优先复用并修复现有：

- `src/components/delete-review/PhotoZoomModal.tsx`

建议实现方式：

- 使用 `react-native-gesture-handler` 的 Gesture API 或现有项目兼容的手势组件。
- 使用 `react-native-reanimated` 管理 `scale`、`translateX`、`translateY`。
- 在预览打开时重置 shared values。
- 对平移范围做边界限制，避免照片被拖出屏幕后无法找回。

### 技术范围

主要修改：

- `src/components/delete-review/PhotoZoomModal.tsx`

可能涉及：

- `src/components/delete-review/DeleteGrid.tsx`
- `app/review.tsx`

### 验收标准

- [ ] Review 页面进入照片预览后，双指捏合可以平滑放大和缩小。
- [ ] 双指点击照片可以在 `1x` 与约 `2x` 之间切换。
- [ ] 放大后可拖动查看照片局部。
- [ ] 关闭预览再打开另一张照片时，缩放状态重置为 `1x`。
- [ ] 缩小低于 `1x` 时自动回弹，不出现照片过小或漂移。
- [ ] 缩放手势不影响 Review 网格的勾选状态。

---

## REQ-08: 更多功能迁移至中间 Tab 并重构 Hub 布局

### 背景

当前个人中心承载了会员、统计、每周回顾、成就、月份分析等多类内容，信息密度越来越高。“更多功能”区域放在个人中心内，会让设置页同时承担“账户 / 统计 / 功能入口”三种职责，结构不够清晰。

底部 Tab 中间已有“更多功能”入口，应将它升级为真正的功能聚合页，把个人中心的更多功能区域全部迁移过去。

### 目标

重构信息架构：

- 个人中心专注账户、订阅、核心统计、设置项。
- 中间 Tab 的“更多功能”页面承载扩展功能、回顾、成就和分析入口。

### 迁移范围

以下模块从 `app/settings.tsx` 迁移到 `app/hub.tsx`：

| 模块 | 迁移后位置 | 说明 |
|---|---|---|
| 月份分析 | 更多功能页顶部主入口 | 作为数据洞察入口 |
| 每周清理回顾 | 更多功能页核心内容区 | 展示最近 7 天清理成果 |
| 清理成就 | 更多功能页核心内容区 | 横向徽章列表或网格 |
| 其他功能占位卡片 | 更多功能页下方 | 为后续能力预留 |

个人中心保留：

| 模块 | 是否保留 | 说明 |
|---|---|---|
| Pro / 订阅状态 | 保留 | 个人中心核心职责 |
| 今日额度 / 开发者模式 | 保留 | 账户和使用限制相关 |
| 四个统计卡片 | 保留 | 作为概览数据 |
| 设置项 / 帮助项 | 保留 | 设置页职责 |
| 更多功能区域 | 移除 | 避免与中间 Tab 重复 |

### Hub 页面布局规划

推荐布局：**“概览 + 回顾 + 成就 + 功能入口”四段式结构**。

#### 1. 顶部概览区

- 页面标题：`更多功能`
- 副标题：`把清理照片变成看得见的成果`
- 顶部可展示一个轻量数据摘要：
  - `本月清理`
  - `连续天数`
  - `释放空间`

#### 2. 月份分析入口

作为第一张大卡片展示：

- 标题：`月份分析`
- 副标题：`看看哪些月份占用了最多记忆空间`
- 视觉：保留或升级当前柱状图概览。
- 点击后行为：如当前已有月份分析内容，则进入 / 展开现有图表；如当前为占位，则保持占位但文案明确。

#### 3. 每周清理回顾

展示现有 `WeeklyReviewCard`：

- 本周浏览
- 本周删除
- 连续天数
- 7 天 mini bar

#### 4. 清理成就

展示现有 `AchievementStrip`：

- 首批成就徽章横向滚动。
- 已解锁高亮，未解锁弱化。
- 保持可扫视，不占据过多垂直空间。

#### 5. 功能入口网格

用于承载后续扩展能力：

| 入口 | 状态 | 说明 |
|---|---|---|
| 最近删除 | 可选保留入口 | 如个人中心不跳转，可在 Hub 内提供入口 |
| 相册洞察 | 占位 | 后续分析不同相册 |
| 清理提醒 | 占位 | 后续本地提醒 |
| 导出报告 | 占位 | 后续生成清理报告 |

### 视觉规则

- 不把每个页面区块都做成大卡片，避免卡片套卡片。
- Hub 页面可以比个人中心更有探索感，但仍保持黑金主视觉。
- 重点模块之间使用明确留白和分区标题。
- 功能入口建议使用 2 列网格，小卡片高度保持一致。
- 页面底部预留 Tab Bar 安全距离，避免最后一行入口被遮挡。

### 技术范围

主要修改：

- `app/settings.tsx`
- `app/hub.tsx`
- `src/components/settings/WeeklyReviewCard.tsx`
- `src/components/settings/AchievementStrip.tsx`

可能涉及：

- `src/components/hub/MonthlyChart.tsx`
- `src/components/settings/SettingsSection.tsx`
- `src/contexts/StatsContext.tsx`

### 验收标准

- [ ] 个人中心不再展示“更多功能”区域。
- [ ] 月份分析、每周清理回顾、清理成就迁移到中间 Tab 的“更多功能”页面。
- [ ] Hub 页面形成清晰的“概览 / 月份分析 / 每周回顾 / 成就 / 功能入口”层级。
- [ ] 从底部中间 Tab 进入 Hub 后，用户能直接看到核心扩展功能。
- [ ] 个人中心页面变得更轻，只保留账户、统计和设置职责。
- [ ] Hub 页面底部内容不被 Tab Bar 遮挡。
- [ ] 迁移后相关数据仍来自现有本地统计，不新增云端依赖。

---

## 13. 补充需求影响范围

| 文件 | REQ-05 | REQ-06 | REQ-07 | REQ-08 |
|---|---:|---:|---:|---:|
| `src/components/delete-review/DeleteConfirmSheet.tsx` | ✓ | ✓ | | |
| `src/utils/delete-confirm-utils.ts` | | ✓ | | |
| `src/services/delete-service.ts` | | ✓ | | |
| `src/components/delete-review/PhotoZoomModal.tsx` | | | ✓ | |
| `src/components/delete-review/DeleteGrid.tsx` | | | ✓ | |
| `app/review.tsx` | ✓ | ✓ | ✓ | |
| `app/settings.tsx` | | | | ✓ |
| `app/hub.tsx` | | | | ✓ |
| `src/components/settings/WeeklyReviewCard.tsx` | | | | ✓ |
| `src/components/settings/AchievementStrip.tsx` | | | | ✓ |
| `src/components/hub/MonthlyChart.tsx` | | | | ✓ |

---

## 14. 补充需求优先级

| 优先级 | 需求 | 原因 |
|---|---|---|
| P0 | REQ-06 Delete 按钮文件大小 0B 修复 | 直接影响用户对删除收益的判断和信任 |
| P0 | REQ-07 Review 缩放能力修复 | 直接影响误删风险，是确认删除前的关键安全能力 |
| P1 | REQ-05 待删除确认页布局上移 | 视觉重心问题明显，属于高感知体验打磨 |
| P1 | REQ-08 更多功能迁移至 Hub | 改善信息架构，避免个人中心继续膨胀 |

---

## 15. 补充验收总清单

- [x] 待删除确认页左上角按钮、标题区、照片堆叠区整体上移，视觉重心自然。
- [x] 有待删除照片时，Delete 按钮后方不再显示 `0B`。
- [x] Review 照片预览支持双指捏合放大 / 缩小。
- [x] Review 照片预览支持双指点击放大 / 还原。
- [x] 放大后可拖动查看局部，关闭后状态重置。
- [x] 个人中心“更多功能”区域全部迁移到中间 Tab 的 Hub 页面。
- [x] Hub 页面按“概览 / 月份分析 / 每周回顾 / 成就 / 功能入口”重新组织。
- [x] 迁移后个人中心职责清晰，不再重复展示 Hub 功能。
- [x] `npx.cmd tsc --noEmit` 通过。
- [x] `npx.cmd jest --runInBand` 通过。

---

## 16. 实施建议

建议按以下顺序开发：

1. 先修复 `REQ-06` 文件大小 0B，这是数据信任问题，且会影响删除确认页展示。
2. 再处理 `REQ-07` Review 缩放，优先保证误删前的确认体验。
3. 然后微调 `REQ-05` 删除确认页布局，结合真实数据展示一起做视觉回归。
4. 最后执行 `REQ-08` Hub 信息架构迁移，避免和前 3 项高优先级修复互相干扰。

开发过程中应保留现有 Expo SDK 54 与项目既有代码模式；只有遇到当前 Expo / Gesture Handler / Reanimated API 行为不确定时，再查官方文档确认。

---

# v1.3 补充需求 — 删除列表提示、相册数据正确性、统计视觉与预览手势修复

> **版本补充**: v1.3.2
> **日期**: 2026-06-03
> **状态**: 已完成
> **来源**: 真机体验反馈
> **目标**: 修复多选删除、相册切换、相册列表过滤和照片预览手势中的功能缺陷，并同步打磨个人中心与月份分析的视觉细节。

## 17. 本轮补充版本目标

本轮补充需求聚焦 6 个高感知问题：

1. 多选照片加入删除列表后，删除确认页仍出现“查看全部 X 张”的提示，造成重复和干扰，需要彻底移除该提示。
2. 切换相册后删除几张照片，明明相册内仍有照片，却直接进入“没有照片存在”的空状态，这是功能性缺陷，需要修复相册内照片池、已浏览记录和删除后补齐逻辑。
3. 相册选择页展示了部分系统相册中不可见或异常的相册，需要按系统相册列表的有效可见相册展示，过滤异常项。
4. 个人中心统计卡片不再使用对应颜色底纹，所有卡片保持黑色背景，仅数值字体使用醒目语义色。
5. 月份分析下方的月份选择图标从居中改为左对齐。
6. 查看照片界面的双击 / 双指捏合缩放仍不可用，需要实现稳定可用的照片放大、缩小和平移查看。

---

## 18. v1.3.2 补充需求清单

| 编号 | 需求名称 | 优先级 | 类型 |
|---|---|---:|---|
| REQ-09 | 删除确认页移除“查看全部 X 张”提示 | P1 | UI / 文案 |
| REQ-10 | 切换相册后删除导致误判空相册修复 | P0 | Bug / 数据流 |
| REQ-11 | 相册选择页过滤异常相册 | P0 | Bug / 数据过滤 |
| REQ-12 | 个人中心统计卡片黑底与数值强调色统一 | P1 | UI |
| REQ-13 | 月份分析月份选择入口左对齐 | P2 | UI |
| REQ-14 | 查看照片双击与双指捏合缩放修复 | P0 | 交互 / 安全确认 |

---

## REQ-09: 删除确认页移除“查看全部 X 张”提示

### 背景

当前删除确认页在部分场景中仍会展示“查看全部 X 张”提示。尤其是用户多选照片加入删除列表后，该提示仍然出现，会让确认页信息显得重复，并暗示还有一个额外查看入口。此前单张照片场景已要求不展示“查看全部 1 张”，本轮需要将该提示在多选场景中也彻底移除。

### 目标

删除确认页不再展示任何“查看全部 X 张”相关提示或按钮文案，让页面只保留必要的删除确认信息、照片预览和操作按钮。

### 交互规则

| 场景 | 目标行为 |
|---|---|
| 单张待删除 | 不展示“查看全部 1 张” |
| 多张待删除 | 不展示“查看全部 X 张” |
| 从浏览页中途快删进入确认页 | 不展示该提示 |
| 从整组滑完进入确认页 | 不展示该提示 |
| 从待删除列表返回确认页 | 不展示该提示 |

### 技术范围

主要修改：

- `src/components/delete-review/DeleteConfirmSheet.tsx`

可能涉及：

- `app/review.tsx`
- `src/utils/delete-confirm-utils.ts`

### 验收标准

- [ ] 任意待删除数量下，删除确认页都不出现“查看全部 X 张”文案。
- [ ] 移除提示后，照片堆叠区域、标题、副标题和按钮布局不出现空洞或错位。
- [ ] 点击照片预览或进入待删除列表的既有入口逻辑如仍保留，必须通过图像本身或明确图标承载，不依赖该文案。
- [ ] 单张、多张、中途删除、整组删除场景均通过真机回归。

---

## REQ-10: 切换相册后删除导致误判空相册修复

### 背景

用户切换到某个相册后，删除几张照片，明明该相册中仍有照片，应用却直接显示“没有照片存在”。这说明当前相册范围内的照片池、已浏览记录、删除后补齐逻辑或空状态判断存在状态不一致问题。

该问题属于核心功能缺陷，会打断照片清理流程，并让用户误以为相册已被清空。

### 目标

修复相册切换后的删除与补齐流程，确保只要当前选中相册仍有可浏览照片，应用就不会误进入空相册状态。

### 问题假设

需要重点排查以下可能原因：

| 可能原因 | 说明 |
|---|---|
| `selectedAlbum` 切换后照片池未正确重建 | 仍沿用全局照片或旧相册缓存 |
| 删除后只从当前组扣减，没有从当前相册剩余照片补齐 | 导致当前组耗尽后误判无照片 |
| 已浏览集合按全局维度过滤，切换相册后误过滤当前相册照片 | 当前相册还有照片，但都被视为已浏览 |
| `loadNextWithLimit` / `generateGroup` 对 album 作用域不一致 | 分组使用的数据源与 UI 空状态判断不一致 |
| 删除后 MediaLibrary 重新拉取时分页或 album 参数丢失 | 当前相册变成空查询结果 |

### 数据规则

| 场景 | 目标行为 |
|---|---|
| 切换相册 | 清晰重建当前相册照片池和当前组 |
| 当前相册删除部分照片 | 从当前相册剩余照片补齐当前组或下一组 |
| 当前相册仍有未展示照片 | 不进入空相册状态 |
| 当前相册照片全部删除或不可访问 | 才展示空相册状态 |
| 切回全部照片或其他相册 | 不受上一个相册的空状态影响 |

### 技术范围

主要排查 / 修改：

- `src/hooks/usePhotoEngine.ts`
- `src/services/photo-service.ts`
- `src/contexts/PhotoContext.tsx`
- `app/index.tsx`
- `app/albums.tsx`

可能涉及：

- `src/services/delete-service.ts`
- `src/types/photo.ts`

### 验收标准

- [ ] 在相册 A 中删除部分照片后，如相册 A 仍有照片，浏览页继续展示剩余照片。
- [ ] 删除后补齐逻辑只从当前选中相册取照片，不混入其他相册。
- [ ] 当前相册确实清空时，才展示空状态。
- [ ] 从相册 A 切到相册 B 后，相册 B 不继承相册 A 的空状态或浏览进度错误。
- [ ] “全部照片”和单个相册之间来回切换，分组和空状态均正确。
- [ ] 单元测试覆盖相册作用域下的分组、删除后补齐、已浏览过滤。

---

## REQ-11: 相册选择页过滤异常相册

### 背景

当前相册选择界面会展示一些异常状况的相册。这些相册可能在系统相册 UI 中不可见、没有有效资源、封面资源缺失、数量异常，或属于系统内部 / 应用缓存类相册。用户希望相册选择页只展示系统相册中正常展示的相册。

### 目标

相册选择页只展示有效、可访问、与系统相册列表一致的相册，过滤异常相册，避免用户进入无效相册后出现空状态或加载异常。

### 相册展示规则

| 相册类型 | 是否展示 | 规则 |
|---|---|---|
| 系统相册中正常可见且有照片 | 展示 | 按现有排序规则展示 |
| 照片数量为 0 | 不展示 | 避免进入空相册 |
| 无法获取封面或首张资源 | 不展示或使用明确占位 | 优先过滤 |
| 权限不可访问相册 | 不展示 | 避免用户点入后失败 |
| 系统内部 / 异常命名 / 无效 ID 相册 | 不展示 | 需根据 MediaLibrary 返回数据过滤 |
| “全部照片”入口 | 保留 | 作为全局浏览入口 |

### 技术建议

- 使用项目已安装 Expo SDK 54 的 `expo-media-library` 能力。
- 优先通过 `MediaLibrary.getAlbumsAsync()` 和针对每个 album 的 `getAssetsAsync({ album, first: 1, mediaType: photo })` 校验有效性。
- 不依赖相册名称硬编码过滤，除非确认为系统固定异常项。
- 过滤规则应封装为独立工具函数，便于单元测试。

### 技术范围

主要修改：

- `app/albums.tsx`
- `src/services/photo-service.ts`

可能新增：

- `src/utils/album-utils.ts`
- `__tests__/unit/album-utils.test.ts`

### 验收标准

- [ ] 相册选择页不展示照片数量为 0 的相册。
- [ ] 相册选择页不展示无法读取首张照片或封面的异常相册。
- [ ] 系统相册中正常展示且有照片的相册仍正常出现。
- [ ] 点击任意展示的相册后，都能进入可浏览照片流程。
- [ ] 不通过大量硬编码相册名实现过滤。
- [ ] 单元测试覆盖空相册、封面缺失、有效相册等过滤规则。

---

## REQ-12: 个人中心统计卡片黑底与数值强调色统一

### 背景

当前个人中心统计卡片中，不同指标存在对应颜色底纹。用户希望取消这些底纹，让统计区域背景统一为黑色，只保留数值字体的醒目颜色。这样可以减少视觉噪音，同时保留关键数据的识别度。

### 目标

统一个人中心统计卡片视觉：所有统计卡片背景为黑色或项目当前统计卡片黑色基底，不再使用红 / 绿 / 黄等 tint 背景；仅数值字体使用语义强调色。

### UI 规则

| 元素 | 目标样式 |
|---|---|
| 卡片背景 | 统一黑色 |
| 卡片底纹 / tint | 移除 |
| 数值字体 | 使用醒目语义色 |
| label 文案 | 保持低强调浅灰 / 白色 |
| 边框 | 如保留，只使用低透明中性色 |

### 色彩规则

| 指标 | 数值颜色 |
|---|---|
| 已浏览 | 白色或浅灰 |
| 最近删除 | 红色系 |
| 连续天数 | 绿色系 |
| 释放空间 | 黄色系 |

### 技术范围

主要修改：

- `src/components/settings/StatCard.tsx`
- `app/settings.tsx`

### 验收标准

- [ ] 个人中心所有统计卡片背景统一为黑色。
- [ ] 统计卡片不再出现对应指标颜色的背景底纹。
- [ ] 各项数值字体仍保留醒目的语义颜色。
- [ ] 卡片布局、字号和间距不因移除底纹发生错位。
- [ ] 暗色背景下所有数值和 label 均具备足够可读性。

---

## REQ-13: 月份分析月份选择入口左对齐

### 背景

当前月份分析下方的选择月份图标处于居中位置。用户希望该图标改为左对齐，使其更符合内容阅读方向和页面层级，避免在视觉上像一个独立居中的主操作。

### 目标

将月份分析下方的月份选择图标 / 入口从居中布局调整为左对齐。

### UI 规则

| 元素 | 目标行为 |
|---|---|
| 月份选择图标 | 左对齐 |
| 月份选择文案 | 如存在，与图标同一行左对齐 |
| 与图表间距 | 保持当前节奏，不因左对齐过度贴边 |
| 点击区域 | 保持足够触摸面积，不小于 44px 高度 |

### 技术范围

主要修改：

- `app/hub.tsx`

可能涉及：

- `src/components/hub/MonthlyChart.tsx`

### 验收标准

- [ ] 月份分析下方的月份选择入口左对齐。
- [ ] 点击区域仍然完整可点。
- [ ] 在不同屏幕宽度下不出现文字截断或图标错位。
- [ ] 调整不影响月份柱状图本身布局。

---

## REQ-14: 查看照片双击与双指捏合缩放修复

### 背景

当前查看照片界面中，双击照片仍无法放大，双指捏合缩放也不可用或不稳定。照片预览是删除前确认细节的关键能力，缩放不可用会增加误删风险。

该需求与 `REQ-07` 同属照片预览缩放能力，但本轮明确要求覆盖“查看照片界面”的双击和双指捏合，需以真机可用为验收标准，不只停留在代码接入。

### 目标

实现稳定可用的照片预览手势：

1. 双击照片可放大 / 还原。
2. 双指捏合可连续放大 / 缩小。
3. 放大后可拖动查看局部细节。
4. 关闭预览或切换照片后，缩放状态重置。

### 交互规格

| 手势 | 行为 |
|---|---|
| 双击照片 | `1x -> 2x`；已放大时还原到 `1x` |
| 双指捏合向外 | 连续放大，最大建议 `4x-5x` |
| 双指捏合向内 | 连续缩小，低于 `1x` 后回弹到 `1x` |
| 放大后单指拖动 | 平移查看局部 |
| 关闭预览 | 重置 scale / translate |
| 再次打开照片 | 初始为 `1x`，居中展示 |

### 技术要求

- 优先使用项目已有 `react-native-gesture-handler` + `react-native-reanimated`。
- 手势实现需避免与关闭按钮、网格长按预览、页面滚动冲突。
- 需要在真机或 dev-client 中验证，不能只依赖静态类型检查。
- 若当前 `PhotoZoomModal` 使用的手势组件不支持所需组合手势，应重构为 Gesture API 的 tap / pinch / pan 组合。

### 技术范围

主要修改：

- `src/components/delete-review/PhotoZoomModal.tsx`

可能涉及：

- `src/components/delete-review/DeleteGrid.tsx`
- `app/review.tsx`

### 验收标准

- [ ] 双击照片可以放大到约 `2x`。
- [ ] 放大状态下再次双击可以还原到 `1x`。
- [ ] 双指捏合可以平滑放大和缩小。
- [ ] 放大后可以单指拖动查看照片局部。
- [ ] 关闭并重新打开照片后，缩放状态重置。
- [ ] 手势不影响待删除列表的多选状态。
- [ ] 至少在 Android 真机或 dev-client 中完成手势回归验证。

---

## 19. v1.3.2 影响范围

| 文件 | REQ-09 | REQ-10 | REQ-11 | REQ-12 | REQ-13 | REQ-14 |
|---|---:|---:|---:|---:|---:|---:|
| `src/components/delete-review/DeleteConfirmSheet.tsx` | ✓ | | | | | |
| `src/hooks/usePhotoEngine.ts` | | ✓ | | | | |
| `src/services/photo-service.ts` | | ✓ | ✓ | | | |
| `src/contexts/PhotoContext.tsx` | | ✓ | | | | |
| `app/index.tsx` | | ✓ | | | | |
| `app/albums.tsx` | | ✓ | ✓ | | | |
| `src/utils/album-utils.ts` | | | ✓ | | | |
| `src/components/settings/StatCard.tsx` | | | | ✓ | | |
| `app/settings.tsx` | | | | ✓ | | |
| `app/hub.tsx` | | | | | ✓ | |
| `src/components/hub/MonthlyChart.tsx` | | | | | ✓ | |
| `src/components/delete-review/PhotoZoomModal.tsx` | | | | | | ✓ |
| `src/components/delete-review/DeleteGrid.tsx` | | | | | | ✓ |
| `app/review.tsx` | | | | | | ✓ |

---

## 20. v1.3.2 优先级建议

| 优先级 | 需求 | 原因 |
|---|---|---|
| P0 | REQ-10 切换相册后删除导致误判空相册修复 | 核心浏览 / 删除流程功能缺陷，直接阻断用户使用 |
| P0 | REQ-11 相册选择页过滤异常相册 | 会导致用户进入无效相册，放大空状态问题 |
| P0 | REQ-14 查看照片双击与双指捏合缩放修复 | 删除前确认细节能力，影响误删风险 |
| P1 | REQ-09 删除确认页移除“查看全部 X 张”提示 | 高感知确认页文案问题，范围较小 |
| P1 | REQ-12 个人中心统计卡片黑底与数值强调色统一 | 视觉统一与用户明确反馈 |
| P2 | REQ-13 月份分析月份选择入口左对齐 | 局部 UI 微调，风险较低 |

---

## 21. v1.3.2 验收总清单

- [ ] 删除确认页任意数量下都不展示“查看全部 X 张”提示。
- [ ] 切换到单个相册后删除部分照片，不会在仍有照片时误显示空相册。
- [ ] 删除后补齐和下一组生成只使用当前选中相册的有效照片。
- [ ] 相册选择页只展示有效可访问、系统相册中正常展示的相册。
- [ ] 个人中心统计卡片全部为黑色背景，仅数值字体使用强调色。
- [ ] 月份分析下方月份选择入口左对齐。
- [ ] 查看照片界面支持双击放大 / 还原。
- [ ] 查看照片界面支持双指捏合放大 / 缩小。
- [ ] 放大后可拖动查看局部，关闭后缩放状态重置。
- [ ] `npx.cmd tsc --noEmit` 通过。
- [ ] `npx.cmd jest --runInBand` 通过。
- [ ] Android 真机或 dev-client 完成相册切换、删除补齐、相册过滤和照片缩放回归。

---

## 22. v1.3.2 实施建议

建议按以下顺序开发：

1. 先修复 `REQ-10` 和 `REQ-11`，这两项都与相册数据源、过滤和空状态判断有关，适合一起排查。
2. 再修复 `REQ-14`，确保删除前查看照片细节的安全能力真实可用。
3. 然后处理 `REQ-09`，移除删除确认页残留提示并做确认页回归。
4. 最后处理 `REQ-12` 和 `REQ-13` 两个视觉细节，统一个人中心与 Hub 局部布局。

开发过程中继续遵守项目约束：使用已安装 Expo SDK 54 和现有代码模式；只有当相册 API、Gesture Handler 或 Reanimated 当前行为无法确认时，再查官方文档。

---

# v1.3 补充需求 — 庆祝动画升级、月份分析可爱化与照片缩放再排查

> **版本补充**: v1.3.3
> **日期**: 2026-06-03
> **状态**: 已完成
> **来源**: 真机体验反馈
> **目标**: 提升删除成功后的情绪反馈，优化月份分析模块的视觉层级与可爱细节，并彻底排查照片预览手势缩放在真机上仍不生效的问题。

## 23. v1.3.3 补充版本目标

本轮补充需求聚焦 3 个方向：

1. 删除成功庆祝动画需要从单一效果升级为 3 组随机动画，每次删除成功随机出现一组，整体动效参考 iOS 线性、克制、流畅的庆祝反馈。
2. 月份分析中的年份选择器和月份图表需要整合进一个大的布局容器，并在年份选择器最右侧增加一个可爱风格的彩色图形，例如小狗爪子或小猫爪子。
3. 查看照片的双击 / 双指捏合缩放在真机上仍未生效，需要进入系统性排查，明确根因并实现真实可用的缩放体验。

---

## 24. v1.3.3 补充需求清单

| 编号 | 需求名称 | 优先级 | 类型 |
|---|---|---:|---|
| REQ-15 | 删除成功庆祝动画升级为 3 组随机动效 | P1 | UI / 动效 |
| REQ-16 | 月份分析年份选择器与图表整合为大容器并增加可爱爪印图形 | P1 | UI / 视觉 |
| REQ-17 | 查看照片手势缩放失效根因排查与修复 | P0 | Bug / 手势交互 |

---

## REQ-15: 删除成功庆祝动画升级为 3 组随机动效

### 背景

当前删除成功后的庆祝动画较单一，用户希望每次完成删除后有更丰富、更精致的反馈。庆祝动画应提升完成感，但不能过度花哨或干扰继续清理照片。

### 目标

将删除成功庆祝动画扩展为 3 组不同视觉表现，每次删除成功随机播放其中一组。整体动效参考 iOS 风格的线性动画设计：轻盈、顺滑、节奏克制，有明确起承转合。

### 动效方向

推荐 3 组动画：

| 动效组 | 视觉主题 | 动作描述 |
|---|---|---|
| A | 金色完成勾 | 金色圆形勾从 0.88 缩放到 1，周围粒子沿弧线轻微散开后淡出 |
| B | 卡片飞走 | 2-3 张小照片卡片向上轻轻滑出并淡化，中心出现“已清理 X 张” |
| C | 星光扫过 | 一条金色高光横向扫过标题，彩色小点从中心向外扩散 |

### 动效规则

- 每次删除成功随机选择一组动画。
- 动画总时长建议 `900ms-1400ms`。
- 动画完成后自动关闭，不需要用户手动关闭。
- 动画不阻塞后续数据状态更新，只作为视觉反馈层。
- 不新增重型动画依赖，优先使用现有 `react-native-reanimated`。

### 视觉规则

- 保持黑色背景 + 金色强调的主风格。
- 可以加入少量彩色粒子，但不能形成杂乱彩虹感。
- 文案保持中文，例如：
  - `完成！已清理 X 张`
  - `相册轻了一点`
  - `这组整理好了`
- 不使用大面积刺眼红色，删除完成后的情绪应偏正反馈。

### 技术范围

主要修改：

- `src/components/ui/CelebrationOverlay.tsx`

可能涉及：

- `app/index.tsx`
- `app/review.tsx`
- `src/design-tokens.ts`

### 验收标准

- [ ] 删除成功后有 3 组不同庆祝动画可随机出现。
- [ ] 同一轮多次删除不会永远只出现同一组动画。
- [ ] 动画整体顺滑，不出现明显卡顿、闪烁或布局跳动。
- [ ] 动画文案与删除数量一致。
- [ ] 动画结束后自动消失，用户可继续浏览。
- [ ] 不新增重型依赖。

---

## REQ-16: 月份分析年份选择器与图表整合为大容器并增加可爱爪印图形

### 背景

当前月份分析中的年份选择器和下方月份图表在视觉上相对分散。用户希望这两个元素被一个更完整的大布局承载，让月份分析成为一个明确的信息模块。同时，在年份选择器最右侧增加类似小狗爪子或小猫爪子的彩色可爱图形，让数据分析区更亲和。

### 目标

将年份选择器与月份图表包裹进同一个大容器中，形成完整的“月份分析面板”。在年份选择器同一行最右侧放置一个可爱、彩色、轻量的爪印图形。

### 布局规则

| 区域 | 目标 |
|---|---|
| 大容器 | 包裹年份选择器和月份图表，形成独立月份分析模块 |
| 年份选择器 | 保持左对齐，位于容器顶部 |
| 可爱图形 | 位于年份选择器所在行最右侧 |
| 月份图表 | 位于年份选择器下方，与容器内边距保持一致 |
| 容器底部 | 保留足够留白，避免柱状图贴边 |

### 可爱图形规格

推荐方案：**彩色爪印图形**。

- 图形可以使用纯 React Native View 组合绘制，不优先引入图片资源。
- 形状由一个掌垫 + 3-4 个小趾垫组成。
- 色彩可以使用金色、粉色、蓝色、绿色中的 2-3 个点缀色。
- 尺寸建议 `34-44px`，不要喧宾夺主。
- 图形只作为装饰，不承担点击功能。

### 视觉规则

- 不做卡片套卡片。
- 大容器背景可使用深黑 / 深灰半透明层，边框低透明。
- 爪印图形要可爱但克制，不能破坏 PickUp 黑金主视觉。
- 年份选择器仍然可点击，触摸区域不小于 `44px` 高度。

### 技术范围

主要修改：

- `app/hub.tsx`

可能新增：

- `src/components/hub/PawMark.tsx`

可能涉及：

- `src/components/hub/MonthlyChart.tsx`
- `src/design-tokens.ts`

### 验收标准

- [ ] 年份选择器和月份图表被同一个月份分析大容器包裹。
- [ ] 年份选择器保持左对齐。
- [ ] 年份选择器行最右侧展示彩色可爱爪印图形。
- [ ] 爪印图形不影响年份选择器点击。
- [ ] 月份图表在容器内布局稳定，不贴边、不溢出。
- [ ] 不引入额外图片资源也能正常渲染。

---

## REQ-17: 查看照片手势缩放失效根因排查与修复

### 背景

用户反馈：查看照片界面通过双击或双指捏合仍无法放大缩小。当前代码中 `PhotoZoomModal` 已使用 `react-native-gesture-handler` 的 `GestureDetector`、`Gesture.Pinch()`、`Gesture.Pan()` 和 `Gesture.Tap().numberOfTaps(2)`，根布局 `app/_layout.tsx` 也已经包裹 `GestureHandlerRootView`。因此问题不太像是根部缺失手势容器，更可能是 Modal 层级、组合手势冲突或手势响应区域问题。

### 初步排查结论

已确认：

- `app/_layout.tsx` 根部存在 `GestureHandlerRootView`。
- `PhotoZoomModal` 由 `app/review.tsx` 渲染，打开条件为 `previewPhoto !== null`。
- 当前 `PhotoZoomModal` 内部使用 `Gesture.Simultaneous(pinchGesture, panGesture, doubleTapGesture)`。
- 手势区域是包裹 `Animated.View zoomStage` 的 `GestureDetector`。

重点怀疑：

| 假设 | 说明 | 排查方向 |
|---|---|---|
| Modal 内 RNGH 手势根不稳定 | React Native `Modal` 可能创建独立原生层级，根部 `GestureHandlerRootView` 不能覆盖内部手势 | 在 Modal 内容内部再包一层 `GestureHandlerRootView` |
| Pan 与 Tap / Pinch 同时参与导致竞争 | `Gesture.Simultaneous` 中 pan 可能抢占或影响 tap / pinch 状态 | 使用 `Gesture.Race` / `Gesture.Exclusive` / `Gesture.Simultaneous` 分层组合 |
| 手势响应区域被绝对定位层影响 | close 按钮、hint bar、badge 或 stage 层级可能影响触摸分发 | 确认非交互装饰层使用 `pointerEvents="none"` |
| `Animated.Image` 变换中心与 stage 尺寸不匹配 | 视觉看起来没有缩放，或缩放被 contain / 父容器裁剪抵消 | 将 transform 应用到包裹层，明确 stage 尺寸和 overflow |
| 双击识别时长过短 | `maxDuration: 260` 在部分 Android 设备上过紧 | 放宽双击时长 / 间隔参数 |

### 目标

彻底修复查看照片界面手势缩放，使双击、双指捏合和放大后拖动在 Android dev-client / 真机上真实可用。

### 交互规格

| 手势 | 行为 |
|---|---|
| 双击照片 | `1x -> 2x`；已放大时还原到 `1x` |
| 双指捏合向外 | 连续放大，最大 `4x-5x` |
| 双指捏合向内 | 连续缩小，低于 `1x` 回弹到 `1x` |
| 放大后单指拖动 | 平移查看局部 |
| 关闭按钮 | 关闭预览 |
| 关闭后再打开 | scale / translate 全部重置 |

### 技术要求

- 必须先在代码中加入可观察的调试手段或最小复现判断，例如临时显示 gesture state / scale badge，确认手势事件是否触发。
- 如果事件完全不触发，优先验证 Modal 内局部 `GestureHandlerRootView`。
- 如果事件触发但视觉不变化，优先检查 animated style 是否应用到正确层级。
- 如果 pinch 可用但 double tap 不可用，单独调整 tap gesture 组合方式。
- 不能只依赖 `tsc` 和 Jest 判断完成，必须通过真机或 dev-client 手势回归。

### 技术范围

主要修改：

- `src/components/delete-review/PhotoZoomModal.tsx`

可能涉及：

- `app/review.tsx`
- `src/components/delete-review/DeleteGrid.tsx`

### 验收标准

- [ ] Android dev-client / 真机上双击照片可放大到约 `2x`。
- [ ] Android dev-client / 真机上再次双击可还原到 `1x`。
- [ ] Android dev-client / 真机上双指捏合可以连续放大 / 缩小。
- [ ] 放大后可拖动查看照片局部。
- [ ] 关闭预览再打开，缩放状态重置。
- [ ] 手势不影响待删除列表多选和长按预览。
- [ ] 修复前后需要记录根因结论，避免后续重复踩坑。

---

## 25. v1.3.3 影响范围

| 文件 | REQ-15 | REQ-16 | REQ-17 |
|---|---:|---:|---:|
| `src/components/ui/CelebrationOverlay.tsx` | ✓ | | |
| `app/index.tsx` | ✓ | | |
| `app/review.tsx` | ✓ | | ✓ |
| `app/hub.tsx` | | ✓ | |
| `src/components/hub/PawMark.tsx` | | ✓ | |
| `src/components/hub/MonthlyChart.tsx` | | ✓ | |
| `src/components/delete-review/PhotoZoomModal.tsx` | | | ✓ |
| `src/components/delete-review/DeleteGrid.tsx` | | | ✓ |

---

## 26. v1.3.3 优先级建议

| 优先级 | 需求 | 原因 |
|---|---|---|
| P0 | REQ-17 查看照片手势缩放失效根因排查与修复 | 删除前确认照片细节，直接影响误删风险 |
| P1 | REQ-15 删除成功庆祝动画升级为 3 组随机动效 | 高频反馈点，提升完成感和产品质感 |
| P1 | REQ-16 月份分析大容器与可爱爪印图形 | 高感知视觉优化，提升 Hub 页完整度 |

---

## 27. v1.3.3 验收总清单

- [ ] 删除成功庆祝动画至少有 3 组不同表现。
- [ ] 每次删除成功随机出现一组庆祝动画。
- [ ] 庆祝动画顺滑、克制，符合 iOS 线性动画风格。
- [ ] 月份分析年份选择器和月份图表位于同一个大容器中。
- [ ] 年份选择器左对齐，最右侧有彩色可爱爪印图形。
- [ ] 爪印图形不影响年份选择器点击。
- [ ] 查看照片双击缩放在 Android dev-client / 真机上生效。
- [ ] 查看照片双指捏合缩放在 Android dev-client / 真机上生效。
- [ ] 放大后拖动查看局部生效。
- [ ] `npx.cmd tsc --noEmit` 通过。
- [ ] `npx.cmd jest --runInBand` 通过。
- [ ] 真机回归记录手势缩放根因与验证结果。

---

## 28. v1.3.3 实施建议

建议按以下顺序开发：

1. 先处理 `REQ-17`，因为这是功能性缺陷。先加调试观察或最小复现，确认是 Modal 手势根、组合手势冲突还是 animated transform 层级问题，再实现修复。
2. 再处理 `REQ-15`，将现有 `CelebrationOverlay` 扩展为 3 组随机动画，避免影响删除数据流。
3. 最后处理 `REQ-16`，重构 Hub 月份分析布局并新增可爱爪印图形。

开发过程中继续遵守项目约束：使用已安装 Expo SDK 54 和现有代码模式；只有当 Gesture Handler / Reanimated 的当前行为无法确认时，再查官方文档。

---

# v1.3 补充需求 — 开屏动效、App 信息页与更新日志弹框

> **版本补充**: v1.3.4
> **日期**: 2026-06-03
> **状态**: 已完成
> **来源**: 产品体验反馈
> **目标**: 提升 App 启动第一印象，补全个人中心关于信息，并在版本更新后主动告知用户本次更新内容。

## 29. v1.3.4 补充版本目标

本轮补充需求聚焦 3 个产品细节：

1. 当前 App 开屏动画较规则、线性和静态，需要优化为更不规则、更灵动的品牌动效。
2. 个人中心“关于”区域需要新增 `PickUp` 选项，点击后展示当前 App 信息、作者信息和联系方式。
3. App 更新后第一次打开时，需要弹出本次更新日志，右下角提供“我已知晓”按钮，用户确认后后续不再重复弹出。

---

## 30. v1.3.4 补充需求清单

| 编号 | 需求名称 | 优先级 | 类型 |
|---|---|---:|---|
| REQ-18 | 开屏动画优化为不规则灵动动效 | P1 | UI / 动效 |
| REQ-19 | 个人中心关于区域新增 PickUp 信息页 | P1 | 功能 / 信息展示 |
| REQ-20 | 更新后首次打开弹出更新日志 | P0 | 功能 / 版本提示 |

---

## REQ-18: 开屏动画优化为不规则灵动动效

### 背景

当前 `SplashScreen` 主要是标题和标语的规则淡入、上移动画，整体偏静态和模板化。用户希望开屏动画更灵动，不要是过于规则的线性出现。

### 目标

优化 App 开屏动画，让 `PICKUP` 品牌出现过程更有生命力：轻微错位、错峰、非规则节奏、柔和回弹，但仍保持 PickUp 黑金风格和高级感。

### 动效方向

推荐方案：**不规则卡片聚合 + 字母错峰显现**。

动效元素：

- 背景保持黑色。
- 2-3 张抽象照片卡片从不同方向轻微漂入，角度和位移不完全一致。
- `PICKUP` 字母或整体标题错峰出现，不是统一淡入。
- 金色小点 / 光线以非规则路径轻轻滑过，不做机械直线扫光。
- 标语 `整理你的生活` 延迟出现，节奏比标题慢半拍。

### 动效规则

| 元素 | 目标行为 |
|---|---|
| 标题 | 不规则错峰淡入 / 轻微位移 |
| 背景卡片 | 轻微漂入、旋转、停在品牌后方或旁侧 |
| 金色点缀 | 小范围弹跳或弧线移动 |
| 标语 | 慢于标题出现，保持克制 |
| 总时长 | 建议 `2200ms-2800ms` |

### 视觉规则

- 不使用过度夸张弹跳。
- 不使用大量彩色元素，金色仍是主强调。
- 不引入真实照片资源，使用抽象卡片或现有品牌图形即可。
- 动画需要在低端 Android 上保持流畅。

### 技术范围

主要修改：

- `src/components/SplashScreen.tsx`

可能涉及：

- `src/design-tokens.ts`
- `assets/`（如确需使用现有 logo / icon，但不优先新增资源）

### 验收标准

- [ ] 开屏动画不再是单一规则淡入。
- [ ] 至少包含 2 种非同步动效元素，例如卡片漂入 + 标题错峰。
- [ ] 动效整体灵动但不杂乱。
- [ ] 开屏完成后正常进入 App，不影响 Provider 初始化。
- [ ] Android dev-client 中启动无明显卡顿。
- [ ] `npx.cmd tsc --noEmit` 通过。

---

## REQ-19: 个人中心关于区域新增 PickUp 信息页

### 背景

当前个人中心“关于”区域只有隐私政策和版本信息，缺少 App 信息、作者信息和联系方式。用户希望新增一个 `PickUp` 选项，点击后展示当前 App 信息和作者信息。

### 目标

在个人中心“关于”区域新增 `PickUp` 入口。点击后展示当前 App 信息、作者信息 `Zack Feng` 和联系方式 `1179722988@qq.com`。

### 信息内容

页面或弹框需展示：

| 字段 | 内容 |
|---|---|
| App 名称 | `PickUp` |
| App 标语 | `记忆由你选择` |
| 当前版本 | 从当前项目版本读取或显示当前配置版本 |
| 作者 | `Zack Feng` |
| 联系方式 | `1179722988@qq.com` |

### 展示方式

推荐方案：**独立关于详情页**。

理由：

- 信息比普通 Alert 更完整。
- 后续可以扩展版权、官网、更新日志入口。
- 与现有 `app/` file-based route 模式一致。

备选方案：自定义 Modal / Alert。范围更小，但扩展性较差。

### 交互规则

- 个人中心 `关于` section 新增一行：`PickUp`。
- 点击进入 `/about` 或打开自定义信息弹层。
- 页面包含返回按钮。
- 邮箱可以作为可复制文本；如实现点击邮件，则使用 `mailto:` 打开系统邮件。
- 不强制联网。

### 技术范围

主要修改：

- `app/settings.tsx`

可能新增：

- `app/about.tsx`

可能涉及：

- `app/_layout.tsx`
- `package.json`
- `app.config.js`

### 验收标准

- [ ] 个人中心“关于”区域新增 `PickUp` 选项。
- [ ] 点击后能看到 App 名称、标语、当前版本、作者、联系方式。
- [ ] 作者显示为 `Zack Feng`。
- [ ] 联系方式显示为 `1179722988@qq.com`。
- [ ] 页面或弹层可返回个人中心。
- [ ] 不影响隐私政策和版本行原有功能。

---

## REQ-20: 更新后首次打开弹出更新日志

### 背景

App 更新后用户需要知道本次更新了哪些内容。目前更新日志只存在于文档中，不会在 App 内主动展示。用户希望更新之后第一次打开 App 时弹出本次更新日志，并提供“我已知晓”按钮。

### 目标

当用户安装新版本并首次打开 App 时，自动弹出本次版本更新日志。用户点击右下角“我已知晓”后，本版本后续启动不再弹出。

### 触发规则

| 场景 | 行为 |
|---|---|
| 首次安装 App | 可展示当前版本更新日志 |
| 从旧版本更新到新版本后首次打开 | 展示新版本更新日志 |
| 同一版本已经点过“我已知晓” | 不再展示 |
| 下次版本号变化 | 再次展示对应版本更新日志 |

### 存储规则

- 使用 AsyncStorage 保存用户已确认的版本号。
- 推荐 key：`acknowledgedChangelogVersion`。
- 对比当前 App 版本，如当前版本与已确认版本不同，则展示弹框。
- 点击“我已知晓”后写入当前版本号。

### 弹框内容

弹框展示：

- 标题：`PickUp 已更新`
- 副标题：`看看这次有什么新变化`
- 更新内容：读取或内置当前版本 changelog 摘要。
- 右下角按钮：`我已知晓`

按钮规则：

- `我已知晓` 位于弹框右下角。
- 点击后关闭弹框并记录当前版本。
- 不需要提供“稍后提醒”。

### 更新日志来源

推荐方案：**App 内维护结构化版本更新内容**。

说明：

- React Native 运行时不适合直接读取根目录 `CHANGELOG.md`。
- 可以在 `src/constants/changelog.ts` 或类似文件中维护当前版本摘要。
- 文档 `CHANGELOG.md` 与 App 内 changelog 需要同步更新。

### 技术范围

可能新增：

- `src/constants/changelog.ts`
- `src/components/ui/ChangelogModal.tsx`

主要修改：

- `app/_layout.tsx` 或根级页面容器

可能涉及：

- `app.config.js`
- `package.json`
- `src/design-tokens.ts`

### 验收标准

- [ ] 当前版本首次启动时弹出更新日志。
- [ ] 用户点击“我已知晓”后，本版本不再重复弹出。
- [ ] 版本号变化后会再次弹出。
- [ ] 弹框内容为中文，且与当前版本更新内容一致。
- [ ] “我已知晓”按钮位于弹框右下角。
- [ ] 弹框不影响 SplashScreen 正常结束和主界面进入。
- [ ] `npx.cmd tsc --noEmit` 通过。
- [ ] `npx.cmd jest --runInBand` 通过。

---

## 31. v1.3.4 影响范围

| 文件 | REQ-18 | REQ-19 | REQ-20 |
|---|---:|---:|---:|
| `src/components/SplashScreen.tsx` | ✓ | | |
| `app/settings.tsx` | | ✓ | |
| `app/about.tsx` | | ✓ | |
| `app/_layout.tsx` | | ✓ | ✓ |
| `src/constants/changelog.ts` | | | ✓ |
| `src/components/ui/ChangelogModal.tsx` | | | ✓ |
| `package.json` | | ✓ | ✓ |
| `app.config.js` | | ✓ | ✓ |

---

## 32. v1.3.4 优先级建议

| 优先级 | 需求 | 原因 |
|---|---|---|
| P0 | REQ-20 更新后首次打开弹出更新日志 | 更新后用户感知最直接，需要版本存储逻辑保证不重复打扰 |
| P1 | REQ-19 个人中心关于区域新增 PickUp 信息页 | 补全产品基础信息和作者联系方式 |
| P1 | REQ-18 开屏动画优化为不规则灵动动效 | 提升 App 第一印象和品牌质感 |

---

## 33. v1.3.4 验收总清单

- [x] 开屏动画包含不规则、错峰、灵动的动效元素。
- [x] 开屏动画完成后正常进入 App。
- [x] 个人中心“关于”区域新增 `PickUp` 选项。
- [x] PickUp 信息页展示当前 App 信息、作者 `Zack Feng` 和邮箱 `1179722988@qq.com`。
- [x] 更新后首次打开 App 自动弹出更新日志。
- [x] 点击右下角“我已知晓”后，本版本不再重复弹出。
- [x] 新版本号变化后，更新日志弹框会再次出现。
- [x] `npx.cmd tsc --noEmit` 通过。
- [x] `npx.cmd jest --runInBand` 通过。

---

## 34. v1.3.4 实施建议

建议按以下顺序开发：

1. 先实现 `REQ-20`，因为它涉及版本号、AsyncStorage 和根布局弹框，需要确保不会干扰启动流程。
2. 再实现 `REQ-19`，新增关于入口和详情页，与个人中心信息架构相关。
3. 最后实现 `REQ-18`，在确认启动链路稳定后打磨开屏动效。

开发过程中继续遵守项目约束：使用已安装 Expo SDK 54 和现有代码模式；只有当版本读取或 Expo Constants 当前行为无法确认时，再查官方文档。

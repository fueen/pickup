# v1.3.5 Polish Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement v1.3.5 polish fixes: remove redundant slogans, optimize delete celebration timing, reload photos for newest-first sorting, and round key icons.

**Architecture:** Keep changes local to existing Expo SDK 54 React Native modules. Add small pure helpers in `photo-service.ts` for TDD coverage, then wire them through `usePhotoEngine`; UI polish stays in current component files.

**Tech Stack:** Expo SDK 54, React Native 0.81.5, expo-router 6, React Native Animated, Jest + ts-jest.

---

### Task 1: Latest Sort Refresh Semantics

**Files:**
- Modify: `src/services/photo-service.ts`
- Modify: `src/hooks/usePhotoEngine.ts`
- Test: `__tests__/unit/photo-service.test.ts`

- [ ] Add failing tests for newest-sort refresh policy and viewed-state reset.
- [ ] Implement pure helpers for `timeNewest` refresh behavior.
- [ ] Wire `changeSortMode('timeNewest', albumId)` to reload the active album and reset the group start.

### Task 2: Slogan Cleanup

**Files:**
- Modify: `src/components/SplashScreen.tsx`
- Modify: `app/settings.tsx`

- [ ] Remove `整理你的生活` from Splash while preserving title/card/dot timing.
- [ ] Replace personal-center footer text with a minimal tappable target that preserves developer-mode five-tap behavior.

### Task 3: Celebration Animation Performance

**Files:**
- Modify: `src/components/ui/CelebrationOverlay.tsx`

- [ ] Reduce animation duration, particle count, and auto-dismiss delay.
- [ ] Stop previous animations and clear timers on repeated visibility changes.
- [ ] Fade out before invoking `onDone` so overlay unmounts quickly.

### Task 4: Rounded Icon Polish

**Files:**
- Modify: `app/_layout.tsx`
- Modify: `app/index.tsx`
- Modify: `src/components/gesture/QuickDeleteButton.tsx`
- Modify: `src/components/photo-card/SortPickerSheet.tsx`
- Modify: selected page icon call sites as needed.

- [ ] Replace high-frequency sharp outline icons with rounder filled or softer MaterialCommunityIcons alternatives.
- [ ] Slightly normalize icon sizes where needed without changing touch targets.

### Task 5: Verification And Review

- [ ] Run targeted photo-service tests.
- [ ] Run `npx.cmd tsc --noEmit`.
- [ ] Run `npx.cmd jest --runInBand`.
- [ ] Review final diff against REQ-21 through REQ-24.

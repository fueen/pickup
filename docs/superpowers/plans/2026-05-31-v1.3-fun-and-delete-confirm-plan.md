# v1.3 Fun and Delete Confirm Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement v1.3's refined delete confirmation dialog, colored settings stats, weekly cleanup review, and achievement strip.

**Architecture:** Keep the feature local-first and UI-focused. Add pure utility functions for weekly review and achievements so behavior is testable without rendering, then consume them from small settings components.

**Tech Stack:** Expo SDK 54, React Native, expo-router, AsyncStorage-backed stats, Jest, React Native Modal, Reanimated.

---

### Task 1: Add Testable Weekly Review and Achievement Logic

**Files:**
- Create: `src/utils/weekly-review-utils.ts`
- Create: `src/utils/achievement-utils.ts`
- Test: `__tests__/unit/weekly-review-utils.test.ts`
- Test: `__tests__/unit/achievement-utils.test.ts`

- [ ] Write failing unit tests for weekly review totals, empty copy, and threshold copy.
- [ ] Write failing unit tests for achievement locked/unlocked states and progress capping.
- [ ] Implement pure utility functions with no React dependencies.
- [ ] Run targeted tests and confirm they pass.

### Task 2: Build Settings Feature Cards

**Files:**
- Create: `src/components/settings/WeeklyReviewCard.tsx`
- Create: `src/components/settings/AchievementStrip.tsx`
- Modify: `src/components/settings/StatCard.tsx`
- Modify: `app/settings.tsx`

- [ ] Extend `StatCard` with `valueColor` and optional subtle tint props.
- [ ] Build `WeeklyReviewCard` using `buildWeeklyReview`.
- [ ] Build `AchievementStrip` using `buildAchievements`.
- [ ] Insert both modules between statistics and help sections.

### Task 3: Redesign Delete Confirmation Sheet

**Files:**
- Modify: `src/components/delete-review/DeleteConfirmSheet.tsx`

- [ ] Replace the basic card layout with a polished dark glass confirmation card.
- [ ] Use a destructive icon, emphasized count, clear copy, confirm and cancel controls.
- [ ] Preserve existing `visible`, `loading`, `onConfirm`, `onCancel` behavior.
- [ ] Avoid native `Alert.alert` for delete confirmation.

### Task 4: Verify and Review

**Files:**
- All changed files

- [ ] Run `npx.cmd tsc --noEmit`.
- [ ] Run `npx.cmd jest --runInBand`.
- [ ] Review changed code for regressions, hidden state bugs, and UI edge cases.
- [ ] Report any residual risks.

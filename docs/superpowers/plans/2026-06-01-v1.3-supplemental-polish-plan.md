# v1.3 Supplemental Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement the v1.3.1 supplemental requirements for delete confirmation layout, non-zero delete size display, Review photo zoom, and Hub feature migration.

**Architecture:** Keep data fixes in service/utils helpers and UI fixes scoped to the existing screens. Reuse existing `DeleteConfirmSheet`, `PhotoZoomModal`, `WeeklyReviewCard`, `AchievementStrip`, and `MonthlyChart` instead of introducing new dependency-heavy components.

**Tech Stack:** Expo SDK 54, React Native 0.81.5, expo-router 6, react-native-gesture-handler, react-native-reanimated 4, Jest + ts-jest.

---

### Task 1: Delete Size Fallback

**Files:**
- Modify: `src/utils/delete-confirm-utils.ts`
- Modify: `src/services/delete-service.ts`
- Test: `__tests__/unit/delete-confirm-utils.test.ts`
- Test: `__tests__/unit/delete-service-restore.test.ts`

- [ ] **Step 1: Write failing tests**

Add tests proving `getDeleteTotalBytes()` estimates non-zero size when `fileSize` is zero and `deletePhotos()` records non-zero freed bytes for zero-size photo input.

- [ ] **Step 2: Run tests to verify failure**

Run: `npx.cmd jest __tests__/unit/delete-confirm-utils.test.ts __tests__/unit/delete-service-restore.test.ts --runInBand`

Expected: tests fail because current helpers sum only `fileSize`.

- [ ] **Step 3: Implement fallback**

Add shared pixel-estimate behavior to delete confirmation utils and ensure delete service uses the same fallback for deletion results.

- [ ] **Step 4: Run tests to verify pass**

Run: `npx.cmd jest __tests__/unit/delete-confirm-utils.test.ts __tests__/unit/delete-service-restore.test.ts --runInBand`

Expected: tests pass.

### Task 2: Delete Confirmation Layout

**Files:**
- Modify: `src/components/delete-review/DeleteConfirmSheet.tsx`

- [ ] **Step 1: Adjust vertical rhythm**

Move the back button, title block, and preview stack upward while keeping the footer pinned safely above the bottom inset.

- [ ] **Step 2: Type-check**

Run: `npx.cmd tsc --noEmit`

Expected: no TypeScript errors.

### Task 3: Review Zoom Gestures

**Files:**
- Modify: `src/components/delete-review/PhotoZoomModal.tsx`

- [ ] **Step 1: Replace ScrollView-only zoom**

Use gesture-handler + reanimated for pinch zoom, pan, and two-finger tap zoom toggle.

- [ ] **Step 2: Type-check**

Run: `npx.cmd tsc --noEmit`

Expected: no TypeScript errors.

### Task 4: Hub Migration

**Files:**
- Modify: `app/settings.tsx`
- Modify: `app/hub.tsx`

- [ ] **Step 1: Remove More Features section from Settings**

Keep membership, stats, help, about, and developer footer in Settings.

- [ ] **Step 2: Build Hub layout**

Move month analysis, weekly review, achievements, and a compact feature grid into Hub. Use `useStatsContext()` and `getValidRecentDeletes()` for local data.

- [ ] **Step 3: Type-check**

Run: `npx.cmd tsc --noEmit`

Expected: no TypeScript errors.

### Task 5: Verification And Self Review

**Files:**
- Review all touched files.

- [ ] **Step 1: Run focused tests**

Run: `npx.cmd jest __tests__/unit/delete-confirm-utils.test.ts __tests__/unit/delete-service-restore.test.ts --runInBand`

- [ ] **Step 2: Run full tests**

Run: `npx.cmd jest --runInBand`

- [ ] **Step 3: Run TypeScript**

Run: `npx.cmd tsc --noEmit`

- [ ] **Step 4: Code review self-audit**

Review diffs for regressions, hidden state bugs, gesture conflicts, unsafe file handling, and UI overlap risks.

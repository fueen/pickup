# v1.3.2 Bugfix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement v1.3.2 fixes for delete confirmation copy, album-scoped browsing, album filtering, stats card styling, month picker alignment, and photo zoom gestures.

**Architecture:** Keep changes inside existing Expo SDK 54 patterns. Add pure utilities for album filtering and group generation so the functional bugs are covered by unit tests; keep UI edits scoped to existing components.

**Tech Stack:** Expo SDK 54, React Native 0.81.5, expo-media-library, react-native-gesture-handler, react-native-reanimated, Jest.

---

### Task 1: Album Filtering Utility

**Files:**
- Create: `src/utils/album-utils.ts`
- Test: `__tests__/unit/album-utils.test.ts`
- Modify: `app/albums.tsx`

- [ ] Write failing tests for filtering empty, coverless, and invalid albums while keeping valid albums.
- [ ] Run `npx.cmd jest --runInBand __tests__/unit/album-utils.test.ts` and confirm RED.
- [ ] Implement `toVisibleAlbumItems()` as a pure mapper/filter.
- [ ] Update `app/albums.tsx` to validate each system album with `getAssetsAsync({ album, first: 1 })` before display.
- [ ] Run the album utility test and confirm GREEN.

### Task 2: Album-Scoped Group Refill

**Files:**
- Modify: `src/services/photo-service.ts`
- Test: `__tests__/unit/photo-service.test.ts`
- Modify: `src/hooks/usePhotoEngine.ts`

- [ ] Write failing tests for deleted/current photos being excluded from refill and no duplicate refill photos being returned.
- [ ] Run `npx.cmd jest --runInBand __tests__/unit/photo-service.test.ts` and confirm RED.
- [ ] Add an `excludedIds` option to `generateGroup()` and use it during quick-delete refill.
- [ ] Reset `groupIndex`, marked sets, and viewed state cleanly when `loadPhotos(albumId)` loads a new album.
- [ ] Run photo-service tests and confirm GREEN.

### Task 3: Delete Confirm and Stats UI

**Files:**
- Modify: `src/components/delete-review/DeleteConfirmSheet.tsx`
- Modify: `src/components/settings/StatCard.tsx`
- Modify: `app/settings.tsx`

- [ ] Remove the “查看全部 X 张” badge from delete confirmation.
- [ ] Remove `tintColor` from `StatCard` behavior and settings usage.
- [ ] Keep only value-level semantic colors.

### Task 4: Hub Month Picker Alignment

**Files:**
- Modify: `app/hub.tsx`

- [ ] Change the year picker button from centered to left-aligned within the month-analysis content column.
- [ ] Preserve touch target size and spacing.

### Task 5: Photo Zoom Gestures

**Files:**
- Modify: `src/components/delete-review/PhotoZoomModal.tsx`

- [ ] Replace two-finger tap-only zoom shortcut with normal double-tap zoom.
- [ ] Keep pinch and pan gestures composed so pinch zoom, double tap, and drag can coexist.
- [ ] Ensure close only happens through the close button, avoiding accidental single-tap close conflicts.

### Task 6: Verification and Review

**Files:**
- All touched files.

- [ ] Run `npx.cmd tsc --noEmit`.
- [ ] Run `npx.cmd jest --runInBand`.
- [ ] Review the diff for regressions against REQ-09 through REQ-14.
- [ ] Report any verification that cannot be performed locally, especially real-device gesture checks.
